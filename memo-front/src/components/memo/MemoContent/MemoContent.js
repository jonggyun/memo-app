import React from 'react';
import styles from './MemoContent.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

const MemoContent = () => (
  <div>
    MemoContent!!!!
  </div>
);

export default MemoContent;